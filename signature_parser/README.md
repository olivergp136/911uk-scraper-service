# 911uk Signature Parser

This service parses 911uk forum member signatures and extracts structured car data.

This is a FastAPI service intended to be deployed as a separate Render web service.

Endpoints:
- GET /
- GET /active
- POST /start
- POST /stop

